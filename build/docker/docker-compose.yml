version: "3"
services:
  solbase:
    image: muellerlab/solbase:1.07
    hostname: solbase
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "1.0"
          memory: 3000M
      restart_policy:
        condition: on-failure
    ports:
      - "3000:3000"
#    volumes:
#      - ./../../sgn_local.conf:/cxgn/sgn/sgn_local.conf
